(author: Chris P)
(version: 0.1)
(date: 01/31/20)

(load tool in spindle safety macro)

o<load_spindle_safety> sub

#<load_spindle_tool_number> = #1

 if [#<load_spindle_tool_number> EQ #5190]
   (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5191]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5192]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5193]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5194]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5195]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5196]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5197]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5198]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5199]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5200]
    (MSG, the tool you are trying to load is already stored in the carousel)
  elseif [#<load_spindle_tool_number> EQ #5201]
    (MSG, the tool you are trying to load is already stored in the carousel)
  else
    M61 Q#<load_spindle_tool_number>
  endif

o<load_spindle_safety> endsub

M2 (end program)
